// EdgeOne Makers discovers Node functions from cloud-functions/api.
// The handler itself is pre-bundled at the functions root so it contains no
// runtime-relative imports when EdgeOne packages this route.
export { default } from '../[[default]].js'
