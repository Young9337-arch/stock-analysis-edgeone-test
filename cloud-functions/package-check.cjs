const fs=require('fs'),path=require('path');const root=__dirname;let bytes=0,files=0,max={size:0,file:''};(function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){if(['node_modules','.git','backups','docs','debug-fixtures'].includes(e.name))continue;const p=path.join(dir,e.name);if(e.isDirectory())walk(p);else{const s=fs.statSync(p).size;bytes+=s;files++;if(s>max.size)max={size:s,file:p}}}})(root);console.log(JSON.stringify({sourceBytes:bytes,files,maxFile:max},null,2))
